import { createContext } from 'react';

// Va en su propio archivo, igual que tabs-context.js y toast-context.js:
// mantiene TreeView.jsx exportando solo componentes.
export const TreeViewContext = createContext(null);
